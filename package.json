{
  "name": "glatam-calendar-monorepo",
  "version": "1.0.2",
  "description": "Monorepo para la librería de calendario independiente @glatam/calendar",
  "private": true,
  "author": "Santiago Hernández Saldarriaga",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "publish:all": "npm publish -w packages/core --access public && npm publish -w packages/ui --access public"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
